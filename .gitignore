# kbuild output
/build/
/.config
/.config.old
/menuconfig.preset

# kbuild is the one cargo project in the tree
/kbuild/target/

# editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# agent worktrees (Claude Code)
/.claude/worktrees/
