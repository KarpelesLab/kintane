[unit]
name = "kintane"
kind = "bin"
root = "src/main.rs"
layer = "kernel"

# `arch` is provided by whichever architecture the configuration selects. The image
# names the role, not the machine.
[deps]
units = ["hal", "arch", "boot_protocol"]
